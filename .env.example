# Envoy Control Plane - Environment Variables Example
# Copy this file to .env.local for development or set these in your production environment

# JWT Secret Key (REQUIRED for authentication)
# Must be at least 32 characters long for security
# Generate a secure key with: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-key-at-least-32-characters-long

# Rust logging level
# Options: error, warn, info, debug, trace
RUST_LOG=info

# Optional: Configuration file path (defaults to config.yaml)
# CONFIG_FILE=config.yaml

# Optional: JWT rotation secret for zero-downtime key rotation
# JWT_SECRET_ROTATION=your-rotation-secret-key

# Production Notes:
# 1. Never commit actual secrets to version control
# 2. Use environment variables or secrets management in production
# 3. Rotate JWT secrets regularly for security
# 4. Consider using JWT_SECRET_ROTATION for zero-downtime key updates